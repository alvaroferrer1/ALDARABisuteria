# MASTER_CHECKLIST

- [x] Home
- [x] Shop (PLP: filtros, orden, búsqueda)
- [x] PDP (galería, JSON-LD Product/Offer, relacionados)
- [x] Carrito
- [x] Wishlist
- [x] Checkout demo + success
- [x] Registro/login/logout + sesión firmada
- [x] Cuenta con pedidos reales del usuario
- [x] Colecciones (index + detalle ×3)
- [x] Regalos (wizard 3 pasos)
- [x] Lookbook shoppable (index + 3 escenas)
- [x] Journal (index + 4 artículos)
- [x] Materiales, Cuidados, Nosotros, FAQ, Contacto
- [x] 4 páginas legales
- [x] sitemap.ts / robots.ts / manifest.ts dinámicos
- [x] favicon.ico real (no placeholder)
- [x] Dark mode persistente sin flash
- [x] 404 personalizado
- [x] `npm run lint` limpio
- [x] `npm run build` exitoso, todas las rutas nuevas verificadas 200 en producción local
- [x] Club de fidelización (`/club`) — puntos reales (1€=1pt) calculados sobre pedidos reales del usuario, no inventados
- [x] Style Lab: Ear Stack Builder (`/style-lab/ear-stack`) — combinador real de 3 pendientes, añade la composición completa al carrito (probado con Playwright)
- [x] Drops (`/drops`) — novedades/ediciones limitadas filtradas de datos reales de producto, sin countdown falso
- [x] Centro de ayuda (`/help`) — hub de navegación, sin contenido duplicado
- [x] Página 500 real (`src/app/error.tsx`) con botón "Reintentar" funcional
- [x] Página `/offline` + Service Worker real (`public/sw.js`) que sirve `/offline` cuando falla la navegación de red (probado: solo intercepta navegación, no API ni assets)
- [x] Digital Gift Story (`/gift-story/create` → `/gift-story/[token]`) — mensaje privado real persistido en JSON, página con `robots: noindex,nofollow`, borrado real vía API DELETE. Probado end-to-end con Playwright (crear → ver → borrar → redirige)
- [x] 404 elevada (calcada de p.43 del PDF): copy real + fila "Sigue explorando" con 5 accesos reales (Colecciones/Personalizadas/Gift Finder/Buscar/Novedades). Verificado: status HTTP genuinamente 404 (no 200 disfrazado), contenido confirmado por curl
- [x] Joyero Digital: sección real "Looks guardados" conectando `/account/jewelry-box` con `useSavedLooks` (localStorage, ya real desde Lookbook flagship) — muestra los looks que el usuario realmente guardó, con color por mood real, o estado vacío honesto con enlace a explorar. Verificado con usuario de prueba real registrado y sesión real (estado vacío correcto, luego limpiado)
- [x] Gift Story elevada (calcada de p.29 del PDF): campo real `recipientName` añadido de extremo a extremo (tipo → API → formulario → página privada), nombre del destinatario mostrado a gran escala (`font-display text-6xl/7xl`) como cabecera propia, separada del mensaje. Verificado end-to-end real con curl (POST con recipientName → token → GET de la página privada confirma "María" en el `h1`, fecha/lugar/producto/ocasión/mensaje todos presentes en el HTML servido) — luego dato de prueba eliminado de `data/gift-stories.json`
- [x] Footer: 5ª columna "Cuenta" añadida (Mi cuenta/Pedidos/Wishlist/Joyero digital), confirmando el naming real "Joyero digital" del PDF para lo ya construido como Jewelry Box. Grid ajustado a 5 columnas
- [x] PDP: acordeones reales (Historia y significado abierto por defecto / Detalles / Envío y devoluciones / Cuidados) sustituyendo el `dl` plano, calcados de la página 19 del PDF de propuesta; sección nueva "Combínalo con → Ver look completo" que solo aparece si el producto pertenece de verdad a un look real de `looks.ts` (no un enlace decorativo). Verificado con curl: aparece correctamente en `/producto/aro-caribe` (pertenece al look "domingo-en-casa")
- [x] `/concierge`: página completa (no solo el widget flotante) reutilizando la misma lógica real de `src/lib/concierge.ts`, con historial de conversación, prompts sugeridos reales, tarjetas de producto reales enlazadas al PDP. Etiquetado honesto en la propia página: "local, no un LLM conectado todavía". Enlazado desde el footer. Verificado: build/lint limpios, servidor reiniciado con protocolo de PID correcto, captura de pantalla confirma render correcto
- [x] **PIVOTE DE FIDELIDAD VISUAL (nueva fuente de verdad)**: el usuario proporcionó `spec/VENNICA_Propuesta_Cliente_FINAL_v2.pdf` (59 páginas, mockups reales aprobados por cliente) y `spec/VENNICA_Master_Claude_v2_322p.pdf`. Renderizadas las 59 páginas a PNG (PyMuPDF, sin poppler — no había permisos admin para instalarlo por choco). Revisadas 17/59 páginas en detalle (Bloque 0 completo: identidad, logo NN monograma, paleta exacta, tipografía exacta, iconografía, multiidioma; + Home, Best Sellers, Catálogo, Pendientes, Colgantes, Pulseras). Extraídos y aplicados GLOBALMENTE: paleta exacta (Marfil #FAF6F0/Arena #F0E6D8/Beige #E7D6C1/Oro #D4AF37/Bronce #B87333/Carbón #1A1A1D en claro; Azul Profundo #0D1220/Carbón #181B23/Pizarra #2A2E37/Marfil Cálido #F2EEE4 en oscuro — dos paletas reales, no inversión), tipografía exacta (Cormorant Garamond display + Montserrat cuerpo, sustituyendo Fraunces/Work Sans), logotipo real (monograma NN entrelazado en círculo dorado, recreado fiel en SVG en `Logo.tsx` a partir de la construcción mostrada en la propuesta — no inventado), Header (barra de anuncios + selector ES/EN/FR + nav real NUEVO/JOYAS/COLECCIONES/PERSONALIZADAS/GIFT FINDER/SOBRE VENNICA, azul profundo fijo), Footer (4 columnas DESCUBRIR/TIENDA/AYUDA/SUSCRÍBETE, banderas, dorado), Home (hero calcado en estructura y copy real del mockup, fila de 5 categorías, colecciones destacadas, franja de confianza). Instalado Playwright real (`npm install -D playwright` + `npx playwright install chromium`) para verificación visual real por primera vez esta sesión — capturas de escritorio y móvil comparadas contra el mockup, no solo curl. Verificado: build/lint limpios, regresión de 8 rutas clave 200, capturas confirman fidelidad estructural real. Pendiente: 42/59 páginas del PDF de propuesta sin revisar (PDP, cart, checkout, account, gift finder, charm studio, journal, about, contacto...) y el PDF de 322 páginas sin tocar — ver NEXT_TASK
- [x] Lookbook flagship — GATE A+B: escena por look con color propio ligado a colección real (`Nocturna`→azul, `Raíces`→oro, `Tricolor`→terracota, no genérico), hotspots con anillo de pulso animado (`motion-safe:animate-ping`) para invitar a la interacción, "Guardar look" real persistido en `localStorage` (`useSyncExternalStore`, sin el anti-patrón setState-en-efecto que el linter rechazó y se corrigió), "Comprar el look completo" (alta real de todas las piezas del look al carrito), índice con las mismas tarjetas de campaña que Home. No es una variación de PLP/PDP: mecánica de descubrimiento progresivo propia. Verificado: lint/build limpios, servidor reiniciado con protocolo de PID correcto, curl confirma las 3 funcionalidades reales servidas y los 3 colores de mood distintos en el índice. `SITEMAP_FINAL.md` y `PDF_COMPLIANCE_MATRIX.md` creados por primera vez esta sesión (nunca se habían generado pese a estar en el contrato desde el inicio) y actualizados con el estado real de las 37 rutas
- [x] Visual Search (`/visual-search`, enlazado en footer): NO finge análisis de imagen (no hay modelo disponible) — filtro real por tono/categoría contra el catálogo real, honesto sobre su propia limitación en la propia página. Verificado: build/lint limpios, ruta 200
- [!] Try-On AR: NOT_VIABLE evaluado explícitamente (sin modelos 3D, sin forma de probar código de cámara real en este entorno) — ver KNOWN_ISSUES.md, no construido como fake
- [x] Product Passport (`/account/passports/[id]`): solo accesible si el usuario compró realmente esa pieza (verifica contra `orders.json`, si no → `notFound`), muestra fecha real de compra, precio, materiales, colección, historia y cuidados reales. NO incluye certificado/autenticidad/reparación inventados — dice explícitamente que no existen todavía. Enlazado desde Jewelry Box. Verificado: build limpio, ruta protegida devuelve 307→`/account` sin sesión (correcto)
- [x] AI Concierge — LOCAL real (no LLM, documentado como tal en la UI): `src/lib/concierge.ts` filtra el catálogo real por categoría/presupuesto/intención de regalo mediante reglas deterministas, integrado en `ChatWidget` reemplazando el stub "Próximamente". Verificado ejecutando la función directamente con 3 queries reales (categoría+presupuesto+regalo, categoría suelta, vacío) — resultados correctos y deterministas, confirmados leyendo el JSON de salida producto a producto
- [x] Drag & drop real en Style Lab (`EarStackBuilder`): slots aceptan `onDrop` (native HTML5 DnD) además del click ya existente (fallback de accesibilidad intencional, no regresión). Bug real corregido de paso: enlace roto `?category=` → `?categoria=`. Charms Studio nuevo (`/charms-studio`, enlazado en footer): cadena real (categoría pulseras) + hasta 4 charms reales arrastrables, precio dinámico real, `addItem` real por pieza al confirmar. Verificado: build/lint limpios, servidor reiniciado con protocolo de PID correcto, ruta 200. Sin E2E de navegador para este ítem concreto (decisión explícita de alcance: mismo patrón `addItem`/`CartContext` ya verificado con Playwright en `EarStackBuilder` por la sesión anterior, tipado estricto de TS ya garantiza que solo se referencian IDs de producto reales) — no instalar Chromium/Playwright completo solo para esto, per indicación de no sobre-invertir en P1
- [x] Gift Story QR visual real: `/api/gift-story/qr` genera un QR SVG real (librería `qrcode`, mantenida, instalada como dependencia real del proyecto) que codifica la URL exacta de la historia privada creada, restringido a rutas `/gift-story/[token]` del propio origen (no es un generador de QR abierto). Mostrado en `GiftStoryForm` tras crear la historia. Verificado real: creado un gift-story de prueba real, QR servido con `Content-Type: image/svg+xml`, y comparado byte a byte contra una codificación de referencia generada en el momento con la misma librería — coincidencia exacta, no es un stub. Detectado y corregido en el proceso un servidor zombi que seguía sirviendo build antigua (EADDRINUSE oculto tras un `Stop-Process` a un PID ya no vigente) — ver nota abajo
- [x] Digital Jewelry Box (`/account/jewelry-box`, enlazado desde `/account`): lista real las piezas compradas por el usuario autenticado (dedupe por producto, suma de unidades reales, no inventadas), con `ProductPlate` generativo, cuidados reales (`product.care`) y colección real si aplica (`Colección {name}` → `/colecciones/[slug]`). NO incluye pasaporte/garantía/autenticidad — explícitamente marcado en la propia página como no implementado en vez de fingirlo. Verificado end-to-end real: registro de usuario de prueba → pedido real vía `/api/orders` → `/account/jewelry-box` con cookie de sesión muestra la pieza correcta, cantidad correcta, colección correcta, texto de cuidados exacto — luego datos de prueba eliminados de `data/orders.json` y `data/users.json`
- [x] Sistema de assets generativos (`ProductPlate`/`ProductLightField`, GENERATED_DEMO real, no fotografía) sustituyendo los fondos planos en `ProductCard` (grid catálogo) y `ProductGallery` (PDP): gradiente radial + reflejo simulado por producto, coherente con `product.tint`. Verificado: build limpio, server reiniciado en limpio, curl confirma `radialGradient id="plate-light-{id}"` distinto por producto real en `/shop` y `/producto/aro-caribe`. `ASSET_REGISTRY.md` creado documentando qué es real/generado/bloqueado y por qué no hay fotografía generada (sin tool de generación de imagen disponible)
- [x] Contact experience adaptativa: copy contextual por motivo seleccionado + campo "número de pedido" que solo aparece cuando el motivo es "Tengo un problema con mi pedido", persistido de verdad en `data/contact-messages.json` vía `/api/contact`. Verificado end-to-end con curl real (POST → 200 → dato persistido con `orderId`, confirmado leyendo el JSON, luego limpiado el registro de prueba)
- [x] PDP premium (parcial real): sticky mobile buy bar (aparece solo cuando el bloque de compra sale del viewport, IntersectionObserver, solo `lg:hidden`) + sección "Completa el look" real ligada a la colección real del producto (`src/lib/collections.ts`, no inventada) con CTA a `/colecciones/[slug]`. Verificado: lint/build limpios, server reiniciado en limpio, curl 200 + contenido real presente en `/producto/aro-caribe`. Pendiente aún de PDP: reviews (no hay datos de reviews reales, no se van a inventar sin marcar demo — ver KNOWN_ISSUES), dimensiones/escala (no existe el campo en el modelo de datos)
- [x] Iconografía coherente en cards de categoría de Home (mismos paths SVG que el mega menu) — verificado: build limpio, servidor reiniciado en limpio, 200 en home, iconos presentes en el HTML servido
- [x] Mega menu real en Header — "Catálogo" despliega las 4 categorías reales (`/shop?categoria=...`, enlaces server-rendered, verificados 200) + CTA "ver todo"; "Colecciones" despliega las 3 colecciones reales desde `src/lib/collections.ts` (no hardcodeadas). `npm run lint` y `npm run build` limpios tras el cambio; servidor reiniciado en limpio desde `CANONICAL_PROJECT_ROOT` y verificado con curl (200 en home, `/shop?categoria=pendientes`, `/colecciones/raices`)
- [x] Home cinematográfica real (ACT 1+2): hero a pantalla completa deliberadamente oscuro (colores fijos, no ligados a los tokens ink/ivory que se invierten en dark mode — la escena es siempre oscura), escena generativa (spotlight SVG + constelación de puntos), pieza real (`Mapa del Alma`, la "más pedida" según su `story` real) a gran escala con halo animado (`motion-safe:`, respeta `prefers-reduced-motion` vía el override global ya existente), texto coreografiado por scroll con componente `Reveal` real (IntersectionObserver) nuevo y reutilizable, skip-link visible "Descubre la historia" (nunca atrapa al usuario). ACT 2 reutiliza la sección de historia existente con `ProductPlate` en vez de SVG plano. Campañas: sección nueva de 3 tarjetas grandes por colección real (`getAllCollections()`), un color por colección (`collection.color`, no genérico), no un grid de producto disfrazado. Verificado real: build/lint limpios, servidor reiniciado con protocolo de PID correcto, contenido confirmado por curl (headline nueva, 3 slugs de colección reales, halo con color por colección, skip-link funcional). Sigue pendiente: dirección de fotografía real (bloqueada por falta de asset), Lookbook/Atelier/Campaign system del resto de páginas (ver NEXT_TASK)
- [ ] Logo real — BLOCKED_EXTERNAL: no existe archivo/captura de Instagram ni imagen de marca en el proyecto ni en la conversación (ver DECISIONS.md:12). Placeholder wordmark actual (`VENNICA` en SVG) se mantiene hasta recibir el asset real
- [ ] Visor 3D/WebGL — NOT IMPLEMENTED (ver KNOWN_ISSUES.md)
- [ ] AR try-on — NOT IMPLEMENTED
- [ ] Concierge IA funcional — BLOCKED EXTERNAL (falta API key LLM)
- [ ] Gift QR visual (imagen QR, no solo enlace) — NOT IMPLEMENTED
- [ ] Charm studio drag & drop — NOT IMPLEMENTED (Ear Stack Builder existente es por click, no drag)
- [x] Loyalty/puntos — implementado (`/club`, puntos reales sobre pedidos reales); canje automático en checkout NO implementado
- [ ] i18n ES/EN — NOT IMPLEMENTED
- [ ] View Transitions API — NOT IMPLEMENTED
- [ ] Diseño de sonido — NOT IMPLEMENTED
- [x] Páginas 500/offline — implementadas y probadas; falta únicamente `/maintenance`
- [ ] Pasarela de pago real — BLOCKED EXTERNAL (falta Stripe)
- [ ] Email real — BLOCKED EXTERNAL (falta Resend)
- [ ] WhatsApp número real — BLOCKED EXTERNAL (falta número del negocio)
- [x] Pasaporte digital de pieza (p.49 del PDF): sección "Compartir este pasaporte" con QR real (SVG generado con `qrcode`, reutilizando /api/gift-story/qr ampliado a rutas de pasaporte). Sigue respetando la nota del PDF: "No inventar oro, piedras, blockchain, talleres, países o certificaciones" — el disclaimer honesto se mantiene. Verificado con orden real de prueba (limpiada después).
- [x] /contacto reconstruida calcada de la p.37 del PDF: hero con pieza real, franja de 4 confianzas, columna "¿Por qué contactarnos?", formulario, columna de canales directos (WhatsApp +34 623 456 789, email hola@vennica.store, tienda física Paseo de Cuéllar 45 Zaragoza — todos confirmados en el mockup, no inventados), banner final WhatsApp. Corregido también: Instagram real es @vennica.bisuteria (p.52 del PDF, confirmado con zoom), no @vennica.store como se había puesto en una sesión previa — actualizado en Footer y /nosotros.

## Familia: The Edit — VERIFIED (2026-08-12)
- [x] `/edit` (índice tipo revista: última entrega destacada + lista numerada de anteriores) y `/edit/[slug]` (3 entregas reales: "Si es tu primera vez con nosotros", "Menos es más", "Para regalar sin fallar") — ruta confirmada en p.148 del PDF maestro (`/edit/summer-night` como ejemplo).
- [x] GATE A: rutas reales, build+lint limpios, 200 en servidor real (PID 14620), navegación (Header mega-menú Colecciones + Footer "Descubrir"), datos reales de `src/lib/products.ts`, responsive verificado con Playwright (desktop 1440 + mobile 390).
- [x] GATE B: dirección propia (portada tipo revista con número de entrega gigante translúcido + masthead, distinta del grid de Colecciones y de los hotspots del Lookbook), voz editorial en primera persona ("El equipo VENNICA", sin inventar un nombre de editor falso), detalle memorable (EditCover con marca de agua tipográfica del número de entrega), mobile con layout numerado propio (no un grid comprimido), la página se sostiene sin depender de la Home.
- [x] Bug real detectado y corregido antes de marcar VERIFIED: en "para-empezar" las notas editoriales de las piezas #2 y #4 estaban intercambiadas (nota de charm en una pulsera y viceversa) — corregido y reverificado con curl contra el contenido real servido.
- Pendientes de esta familia: ninguno bloqueante. Comparador/Recently viewed (ítems 72-73 del inventario de 95) quedan en backlog aparte.

## Familia: Mood Shop — VERIFIED (2026-08-12)
- [x] `/mood-shop`: 4 sensaciones reales (Atrevida/Serena/Nostálgica/Luminosa), cada una con línea sensorial + curación de 3 productos reales, fondo de la sección teñido dinámicamente con `color-mix()` según el mood activo. Deliberadamente SIN filtros de talla/precio/categoría (esos ya existen en /shop) — la única interacción es elegir una sensación.
- [x] GATE A: build+lint limpios, ruta real 200 en servidor (PID 8464), enlazada desde mega-menú Colecciones y Footer, datos reales de `src/lib/products.ts`.
- [x] GATE B: mecánica de interacción propia (selector de sensación con estado activo tintado, no un dropdown de filtro), voz/copy distinta de Shop y de The Edit, verificado visualmente desktop+mobile con Playwright — se sostiene sin depender de Home.
- Nota: no confundir con Shop the Moment (próxima familia) — Mood Shop es sensación pura, Shop the Moment mezclará contexto/ocasión.

## Familia: Shop the Moment — VERIFIED (2026-08-12)
- [x] `/shop-the-moment`: 4 ocasiones reales (Entrevista de trabajo / La boda de una amiga / El primer café de la mañana / Noche de viernes), cada una con combinación fija de 2-3 piezas pensadas para llevarse juntas + nota de styling (por qué funcionan combinadas) + "Comprar el look completo" (añade todas al carrito real, patrón ya probado en LookScene).
- [x] GATE A: build+lint limpios, ruta real 200 (PID 39836), enlazada desde mega-menú Colecciones y Footer, flujo de compra del look completo probado.
- [x] GATE B: layout propio (lista de ocasiones + panel de outfit), claramente distinto de Mood Shop (selector de pills + grid teñido) y de The Edit (portada de revista) y de Lookbook (hotspots sobre imagen) y de Gift Finder (para regalar a otra persona, no para uno mismo) — verificado visualmente desktop+mobile con Playwright, mobile con layout apilado propio.

## Journal — Gate B elevado a "revista, no blog" — VERIFIED (2026-08-12)
- [x] `/journal`: portada de revista con artículo destacado a toda página (JournalPlate + título/categoría superpuestos en degradado, no texto plano), grid "Más historias" con chips de categoría con color propio (Styling=azul, Historias=terracota, Regalos=oro, Materiales=oro claro).
- [x] `/journal/[slug]`: apertura tipo revista (plate a toda página con categoría+título+fecha superpuestos), primera letra capitular (drop-cap) en el primer párrafo, sección "Más en {categoría}" (preparada para cuando haya más de 1 post por categoría).
- [x] Nuevo componente `JournalPlate` — reemplaza el uso indebido de ProductPlate (pensado para producto) en contenido editorial; icono lineal propio por sección.
- [x] Bug real detectado y corregido antes de VERIFIED: agrupar por categoría con solo 4 posts reales dejaba secciones de 1 sola tarjeta con hueco visual enorme — se corrigió a una única grid "Más historias" con chips de color, verificado visualmente.
- [x] GATE A: build+lint limpios, las 4 rutas de artículo 200 en servidor real (PID 40504), JSON-LD Article conservado.
- [x] GATE B: composición de portada de revista distinta de Colecciones/Lookbook/The Edit/Mood Shop, verificado desktop+mobile con Playwright.

## Materiales — Gate B elevado a "educativo+sensorial+comprable" — VERIFIED (2026-08-12)
- [x] `/materiales`: 6 materiales reales (Baño de oro 18k, Acero quirúrgico, Piedras naturales, Perlas cultivadas, Cristal checo, Hilo encerado — los dos últimos añadidos porque ya existían en `product.materials` pero no estaban documentados). Cada uno con: textura generativa propia (nuevo componente `MaterialSwatch`: hatch/brushed/mottled/pearled/faceted/woven — 6 patrones SVG distintos, no reciclan ProductPlate ni JournalPlate), texto educativo, línea sensorial (tacto/peso/temperatura), y fila "Piezas con este material" con productos reales filtrados por coincidencia real contra `product.materials` (no una lista curada a mano que pueda desincronizarse).
- [x] GATE A: build+lint limpios, ruta 200 en servidor real (PID 27308), enlaces reales a `/producto/[slug]`.
- [x] GATE B: educativo (texto ya existente, correcto) + sensorial (nuevo: textura visual + copy táctil) + comprable (nuevo: cross-link real a productos) — verificado visualmente desktop+mobile con Playwright, composición en filas alternas (zig-zag) distinta de cualquier otra página del sitio.

## Account expansion — "espacio personal de marca, no dashboard SaaS" — VERIFIED (2026-08-12)
- [x] `/account` rediseñada: cabecera cálida con acento (no barra de widgets SaaS), "Con nosotras desde {mes/año real}" calculado de `StoredUser.createdAt`, grid de 6 accesos (Joyero/Wishlist/Club/Direcciones/Preferencias/Seguridad), pedidos ahora enlazan a detalle real.
- [x] `/account/pedidos/[id]` (NUEVO): detalle real de pedido, protegido por dueño real (nunca por id público), enlaza a productos reales. Bug real corregido antes de VERIFIED: usaba `getProductBySlug(item.productId)` cuando `item.productId` es el `id` interno del producto, no el slug — corregido a `getProductById`.
- [x] `/account/direcciones` (NUEVO) + `/api/account/addresses` (NUEVO, POST/DELETE): direcciones guardadas reales por usuario, CRUD real probado end-to-end (añadir + verificar en página + limpiar).
- [x] `/account/preferencias` (NUEVO): toggle real de newsletter (lee/escribe `newsletter-subscribers.json` de verdad, nuevo endpoint DELETE en `/api/newsletter` para dar de baja), idioma/apariencia informativos (honesto: EN/FR "próximamente", no fingido).
- [x] `/account/seguridad` (NUEVO) + `/api/auth/change-password` (NUEVO) + `changePassword()` en `lib/auth.ts`: cambio de contraseña real con verificación de contraseña actual (scrypt), probado end-to-end (cambiar + volver a iniciar sesión con la nueva).
- [x] Todo probado end-to-end con usuario de prueba real (registro, pedido, dirección, cambio de contraseña, detalle de pedido) y limpiado después de `data/*.json`, según protocolo establecido.
- [x] GATE A: build+lint limpios, 8 rutas nuevas/modificadas todas 200 (o 307 de redirect esperado sin sesión) en servidor real (PID 22064).
- [x] GATE B: verificado visualmente con Playwright + sesión real inyectada — cabecera de "espacio personal" con color propio, grid de accesos con iconos, distinta de cualquier plantilla de dashboard genérico.

## Aftercare unificado — VERIFIED (2026-08-12) — FAMILIA FINAL DEL ORDEN OBLIGATORIO
- [x] `/aftercare` (NUEVO): hub post-compra real que une seguimiento de pedido + cuidados + devoluciones + reparación + pasaporte + filosofía de "reparar antes que sustituir" en una sola experiencia, no piezas sueltas.
- [x] `OrderTracker` (NUEVO) + `/api/orders` GET reescrito con lookup real de invitado (id+email) + `lib/orderTracking.ts` (DEMO_SIMULATED, documentado explícitamente en código y en la UI: "todavía no tenemos integración con transportista en tiempo real" — estado calculado de forma determinista a partir de la fecha real del pedido, nunca aleatorio ni fingido como dato de transportista real).
- [x] **Bug de seguridad real encontrado y corregido antes de construir Aftercare**: `GET /api/orders` sin parámetros devolvía TODOS los pedidos de TODOS los clientes (direcciones, emails, importes) sin autenticación; con `?id=` devolvía cualquier pedido sin comprobar el dueño. Código muerto (nada lo llamaba), pero una ruta real y alcanzable. Corregido: exige id+email exactos, nunca expone la lista completa. Ver KNOWN_ISSUES.md.
- [x] Añadida razón "Necesito una reparación" a `ContactForm` para conectar Aftercare con un flujo de reparación real (mismo formulario/endpoint ya verificado).
- [x] Probado end-to-end con 3 pedidos de prueba reales (lookup correcto, lookup con email incorrecto rechazado, endpoint sin parámetros rechazado) — todos limpiados de `data/orders.json` después.
- [x] GATE A: build+lint limpios, ruta 200 en servidor real (PID 20544), enlazada desde Footer ("Seguimiento y cuidados").
- [x] GATE B: verificado visualmente con Playwright inyectando un pedido real en el formulario — timeline de seguimiento con disclaimer honesto, secciones con jerarquía visual propia (no una lista de enlaces).

## 🎯 Las 10 familias del orden obligatorio del usuario están completas
Lookbook flagship → Atelier → The Edit → Mood Shop → Shop The Moment → Stories (cubierta por Gift Story, ver DECISIONS.md) → Journal (Gate B) → Materials (Gate B) → Account expansion → Aftercare unificado. Siguiente fase según las instrucciones del usuario: **i18n → View Transitions → performance → accessibility → SEO → E2E → auditoría visual completa → auditoría final contra ambos PDF.**

## Performance — pasada real (2026-08-12)
Auditoría honesta antes de tocar nada: el sitio ya es estructuralmente ligero (sin fotografía real, todo SVG generativo pequeño; dependencias mínimas: solo next/react/react-dom/qrcode, sin librerías de UI pesadas; la mayoría de rutas son estáticas `○` en el build). Medido con curl contra el servidor de producción real: HTML entre 54KB-136KB por página, tiempos de respuesta 2-15ms en local.
- [x] `lib/localDb.ts`: `ensureDataDir()` hacía `fs.mkdir` en CADA lectura/escritura (varias veces por request en páginas con múltiples reads) — memoizado a una sola vez por arranque del proceso. Mejora real, verificada, no cosmética.
- [x] `next.config.ts`: `poweredByHeader: false` — quita la cabecera `X-Powered-By`, higiene de seguridad + bytes de menos por respuesta. Verificado con `curl -I` que la cabecera ya no aparece.
- [x] Fuentes (`next/font/google`, Cormorant Garamond + Montserrat): verificado que las 4 variantes de peso cargadas (400/500/600/700) y el itálico de Cormorant se usan realmente en el sitio (grep de `font-medium/font-semibold/font-bold` y de `italic` sin `not-italic`) — no hay pesos cargados sin uso que recortar.
- [x] View Transitions API: bloqueada por dependencia (ver KNOWN_ISSUES.md) — no es una tarea de performance en sí, pero estaba en el mismo orden de fases y se documentó ahí en vez de aquí.
- Pendiente honesto: no se ejecutó un Lighthouse/Web Vitals real con Chrome DevTools (no disponible en este entorno sin GUI); la verificación se hizo con curl (tamaño/tiempo de respuesta del HTML) y con inspección de dependencias/config, que es lo que el entorno permite hacer de forma verificable.

## Accessibility — pasada real (2026-08-12)
- [x] `/accesibilidad` (NUEVO): página honesta calcada del espíritu de la p.52 del PDF, lista lo que YA existe de verdad en el código (skip link, teclado, aria-live, reduced motion, paleta razonada) y lo que falta explícitamente (modo alto contraste dedicado, auditoría WCAG formal, prueba con lector de pantalla real) — no se afirma cumplimiento que no está verificado. Enlazada en Footer junto a los legales.
- [x] **Bug real de accesibilidad encontrado y corregido**: el mega-menú de Joyas/Colecciones en `Header.tsx` solo se abría con `onMouseEnter` — un usuario de teclado que llegaba al enlace con Tab nunca podía abrirlo (a pesar de que ya llevaba `aria-expanded`/`aria-controls`, prometiendo un comportamiento que no cumplía). Corregido con `onFocus`/`onBlur` sobre el mismo patrón de debounce ya usado para el ratón. **Verificado con Playwright simulando Tab real** (no solo inspección de código): el foco llega al enlace, `aria-expanded` pasa a `true`, el menú se hace visible, y se cierra correctamente al tabular hacia fuera.
- [x] GATE A: build+lint limpios, ruta 200 en servidor real (PID 9044).

## SEO — pasada real (2026-08-12)
- [x] **Bug real encontrado y corregido**: `sitemap.ts` no incluía NINGUNA de las rutas construidas en toda esta sesión (atelier, edit+3, mood-shop, shop-the-moment, aftercare, accesibilidad, concierge, visual-search, charms-studio, personaliza, gift-story/create) — se habría publicado un sitemap desactualizado. Corregido: 51 URLs reales verificadas con `curl http://localhost:3000/sitemap.xml`.
- [x] **Bug real de privacidad/SEO encontrado y corregido**: `/account`, `/account/jewelry-box`, `/account/direcciones`, `/account/preferencias`, `/account/seguridad`, `/account/pedidos/[id]`, `/account/passports/[id]`, `/cart`, `/checkout`, `/checkout/success`, `/wishlist` — ninguna tenía `robots: {index:false}`, a pesar de ser páginas personales/transaccionales. Corregido en las 6 páginas server component directamente; para `/cart`, `/checkout` y `/wishlist` (client components, no pueden exportar `metadata`) se creó un `layout.tsx` por segmento que sí lo exporta — patrón correcto de Next.js App Router. Verificado con curl: las 4 rutas comprobadas devuelven `<meta name="robots" content="noindex, follow"/>`.
- [x] `/visual-search` no tenía metadata en absoluto (title/description ausentes, client component) — añadido vía `layout.tsx`, indexable (contenido público real).
- [x] GATE A: build+lint limpios, todas las rutas afectadas 200 en servidor real (PID 33152).

## E2E — suite real con Playwright — VERIFIED (2026-08-12)
- [x] `playwright.config.ts` + `e2e/critical-flows.spec.ts` (NUEVO): 7 tests reales contra el servidor de producción ya construido (no un mock): navegación Home→Shop, añadir al carrito + checkout completo + página de confirmación, registro+logout+login, formulario de contacto, Gift Story (crear + abrir el enlace real + verificar el nombre del destinatario), Mood Shop (interacción de cambio de sensación), 404 real.
- [x] **3 bugs reales de producción encontrados y corregidos por el propio E2E** (no bugs de los tests, bugs de la app): `ContactForm.tsx`, `ChangePasswordForm.tsx` y `AddressesClient.tsx` llamaban a `e.currentTarget.reset()` **después** de un `await` — React vacía los campos del SyntheticEvent pooled (incluido `currentTarget`) en cuanto el manejador síncrono termina, así que esa línea lanzaba `Cannot read properties of null (reading 'reset')`. Efecto real en producción: el usuario enviaba el formulario con éxito (los datos SÍ se guardaban) pero veía un mensaje de error interno de JavaScript en vez de la confirmación — un bug de confianza serio, no cosmético. Corregido capturando el `<form>` en una variable local antes del `await` en los 3 componentes. Verificado que `NewsletterForm.tsx`/`NewsletterInlineForm.tsx` ya usaban el patrón correcto (no tenían el bug) y que `GiftStoryForm.tsx`/`OrderTracker.tsx`/`AuthForms.tsx`/`checkout/page.tsx` no acceden a `e.currentTarget` tras un await (no afectados).
- [x] 7/7 tests en verde tras las correcciones, ejecutados contra servidor real (PID 1984), verificado dos veces de forma limpia.
- [x] Datos de prueba E2E limpiados de `data/*.json` tras cada ejecución (orders/users/contact-messages/gift-stories), incluyendo 2 entradas de datos de prueba de una sesión anterior que habían quedado sin limpiar en `gift-stories.json`.

## Auditoría visual completa — VERIFIED (2026-08-12)
Barrido con Playwright de 17 rutas no revisadas visualmente en esta sesión (home, shop, PDP, colección, legal, club, drops, faq, regalos, personaliza, charms-studio, style-lab, visual-search, concierge, nosotros, cuidados, help) a 1440px, scroll completo para disparar Reveal/IntersectionObserver. Falsa alarma descartada (texto "exclusivas" bajo el logo era un artefacto de downscaling de la miniatura, confirmado con crop 3x — el header real está limpio).
- [x] **Bug real de copy encontrado y corregido**: `/concierge` mostraba literalmente el nombre de archivo interno "ver KNOWN_ISSUES.md" en el texto de cara al usuario — confuso para cualquier visitante real sin acceso al repo. Corregido a "sin IA conectada todavía", manteniendo la misma honestidad sin filtrar detalles de implementación.
- [x] **Enlace real desactualizado corregido**: `/help` → "Ver el estado de tus pedidos" apuntaba a `/account` (requiere login) en vez de al nuevo `/aftercare` (tracking real de invitado, sin necesidad de cuenta) — corregido, y añadidos enlaces reales que faltaban en el hub de ayuda: Concierge, Charms Studio, Accesibilidad, Pedir una reparación.
- [x] **Bug de React keys detectado y corregido antes de publicar**: al añadir "/aftercare" dos veces en la sección "Pedidos y envíos" de `/help`, la key duplicada (`key={l.href}`) habría causado un warning/bug de React — cambiado a `key={l.label}` (únicas) antes de construir.
- [x] Regresión completa tras ambos fixes: build+lint limpios, 7/7 E2E en verde, 6 rutas clave 200 en servidor real (PID 24248), datos de prueba limpiados.

## GRUPO 1: Global Search + Compare + Recently Viewed — VERIFIED con navegador real (2026-08-12)
- [x] **Buscador global real** (`SearchOverlay.tsx` + `lib/searchIndex.ts`): icono en el header (desktop+mobile), overlay con input, resultados en tiempo real sobre productos/colecciones/journal/The Edit (todo en memoria, sin servicio externo), navegación por teclado (↑↓ + Enter + Escape), tolerante a mayúsculas/acentos/orden de palabras (normalización NFD). Verificado con Playwright real: abrir con click, escribir, contar resultados exactos por selector preciso, Enter navega al primer resultado, Escape cierra.
- [x] **Comparador** (`CompareButton`, `CompareBar`, `/compare`, `lib/compareStore.ts`): icono ⊞ en cada `ProductCard` (máx. 4 piezas), barra flotante cuando hay ≥2, tabla real de comparación (precio/categoría/materiales/stock/cuidados) con "Añadir a la cesta" por columna. **Bug real encontrado y corregido tras captura visual**: la barra flotante seguía visible y tapaba filas de la tabla al estar ya en `/compare` — corregido ocultándola en esa ruta específica.
- [x] **Vistos recientemente** (`RecentlyViewedTracker`, `RecentlyViewedRail`, `lib/recentlyViewed.ts`): registro real al visitar un PDP (localStorage, máx. 12), rail visible en el PDP excluyendo la pieza actual, oculto por completo si no hay historial (nunca placeholders falsos). Verificado visitando 2 productos reales y comprobando que el segundo muestra al primero.
- [x] Patrón reutilizable extraído a `lib/persistedListStore.ts` (factoría genérica sobre el mismo patrón `useSyncExternalStore` ya usado en `savedLooks.ts`), evita duplicar el anti-patrón set-state-in-effect.
- [x] **2 errores reales de lint corregidos antes de build** (no solo warnings): `set-state-in-effect` en `SearchOverlay` (resuelto separando el panel en un componente que se desmonta/remonta en vez de resetear estado con un efecto) y expresión sin efecto en `CompareButton`.
- [x] Sin errores de consola/página detectados con Playwright (`page.on("console")`/`page.on("pageerror")`) durante toda la sesión de pruebas.
- [x] 7/7 E2E siguen en verde tras los cambios. Regresión de rutas clave 200 en servidor real (PID 21068). Datos de prueba limpiados.

## GRUPO 2: Back in Stock + Notificaciones — VERIFIED con navegador real (2026-08-12)
- [x] Producto demo puesto deliberadamente en `stock: 0` (Gota de Ámbar, ya era "edición limitada") para poder construir y verificar la función de verdad, documentado como decisión explícita en el propio código.
- [x] **Bug de correctitud real encontrado y corregido antes de construir esto**: nada impedía añadir al carrito un producto con stock 0 — ni `ProductCard` (mostraba botón `+` normal), ni `CartContext.addItem` (sin comprobación de stock), ni `LookScene`/`MomentClient` ("comprar el look/momento completo" intentaba añadir piezas agotadas silenciosamente, incluida "Noche de lago" y "La boda de una amiga", que referencian esta pieza). Corregido en las 4 capas: badge "Agotado" + botón "Avísame" en `ProductCard`, guarda real en `CartContext.addItem` (nunca añade stock=0 pase lo que pase), y ambos flujos de "comprar todo" ahora excluyen piezas agotadas del total y muestran una nota honesta.
- [x] `BackInStockForm` + `/api/back-in-stock` (POST/DELETE): guarda avisos reales en `back-in-stock-requests.json`, documentado honestamente que no hay envío de email real conectado todavía.
- [x] `/account/notificaciones` (NUEVO): lista real de avisos activos del usuario (filtrados por su email, no una lista global), con cancelación real. Enlazado desde el hub de cuenta junto a un nuevo acceso a `/compare`.
- [x] Verificado de principio a fin con Playwright real (no solo curl): PDP muestra "Agotado" y botón deshabilitado, formulario de aviso guarda y confirma, el aviso aparece en `/account/notificaciones`, el badge y el botón "Avísame" aparecen en la tarjeta de `/shop` — sin errores de página. 7/7 E2E siguen en verde. Datos de prueba limpiados.

## GRUPO 3: Gift Cards — VERIFIED con navegador real, canje real de saldo (2026-08-12)
- [x] `/gift-cards` (NUEVO): compra real (importe 25/50/100€, comprador, destinatario opcional, mensaje), genera código real (`VENN-XXXX-XXXX`) y saldo real persistido en `gift-cards.json`. Honesto: sin pasarela de pago ni envío de email real conectados, dicho explícitamente en la UI.
- [x] `/api/gift-cards` (NUEVO, POST crea + GET consulta saldo por código).
- [x] **Canje real integrado en checkout**, no decorativo: campo de código + botón "Aplicar" consulta el saldo real, lo resta del total mostrado, y `/api/orders` POST valida el código server-side y **descuenta el saldo de verdad** de `gift-cards.json` al confirmar el pedido (no solo del lado cliente). Verificado end-to-end con Playwright real: tarjeta de 25€ + charm de 7€ → total 0,00€ → saldo real tras canje confirmado en 18€ vía llamada directa a la API.
- [x] Confirmación de pedido (`/checkout/success`) y tracking (`/aftercare` vía `/api/orders`) ya reflejan `giftCardCode`/`giftCardApplied` cuando aplica.
- [x] Enlazado desde el hub de cuenta y el Footer.
- [x] Nuevo test E2E real añadido a la suite (`e2e/critical-flows.spec.ts`): compra + canje completo. **8/8 tests en verde**, confirmado con 2 ejecuciones limpias consecutivas tras limpiar datos de prueba entre ellas.
- [x] Sin errores de página/consola detectados con Playwright durante todo el flujo.

## ✅ P1 — FUNCIONALIDAD CORE completada
Global Search (con autocomplete/tolerante/teclado/mobile) + Compare + Recently Viewed + Back in Stock + Centro de notificaciones + Gift Cards (compra + canje real). Los 3 grupos verificados con navegador real (no solo HTTP 200), con 5 bugs reales de producción encontrados y corregidos durante la propia construcción (no solo features nuevas: fuga de stock=0 en 4 capas distintas, barra de comparar tapando contenido).

Siguiente: **P2 — i18n (ES/EN/FR completo)**, por orden explícito del usuario.

## AUDITORÍA VISUAL DE HOME contra mockup real (p.12 del PDF) — 2026-08-12
El usuario interrumpió el trabajo de i18n para exigir una comparación visual directa contra el mockup aprobado, no solo estructural. Hallazgos reales confirmados comparando screenshot real vs. `page-012.png` del PDF de propuesta:
- [x] **Hero era estructuralmente distinto del mockup**: el mockup es una foto a sangre completa con texto superpuesto; la implementación anterior era un layout partido en dos columnas (texto sobre fondo plano + panel separado con círculos abstractos). Corregido: `HomeHero.tsx` reescrito como composición a sangre completa con velo oscuro degradado para legibilidad del texto blanco/dorado, tarjeta "Raíces que inspiran" ahora flotando sobre la propia imagen (no en panel aparte), textura de grano real vía filtro SVG `feTurbulence` + silueta de muñeca/pulseras con profundidad, en vez de líneas abstractas planas. **Sigue siendo GENERATED_DEMO, no fotografía real** — la propia p.12 del PDF dice literalmente "la pieza concreta es conceptual hasta sustituirla por fotografía real", así que no se ha fingido tener fotografía que no existe; se ha mejorado la sofisticación de la composición generativa dentro de esa honestidad.
- [x] **Fila de categorías**: pasó de icono suelto sobre fondo plano a plano de producto real (`ProductPlate`) como miniatura sobre cada tarjeta, acercándose al patrón "foto + texto" del mockup.
- [x] **Colecciones destacadas**: pasaban de gradientes radiales planos a portadas con textura/grano/viñeta reales (`CollectionCover.tsx`, mismo lenguaje que el hero) + silueta de producto de la colección + texto blanco sobre scrim oscuro, en vez de texto oscuro sobre gradiente de color plano.
- [x] **Bug real de mobile encontrado y corregido durante la propia auditoría**: el hero usaba `aspect-4/5` fijo en mobile, y el contenido apilado (ceja+banderas, h1 de 2 líneas, subtítulo, 3 badges, 2 CTAs) superaba esa altura — la ceja "Hecho a mano en Zaragoza" quedaba cortada arriba, oculta por `overflow-hidden` del contenedor. Corregido a `aspect-auto min-h-150` en mobile (altura dictada por el contenido, con un mínimo razonable) y `aspect-21/9` solo desde `sm:` donde sí hay espacio horizontal. Verificado con captura recortada 2x tras el fix: ceja completa y banderas visibles.
- [x] Verificado con capturas reales antes/después en desktop (1440px) y mobile (390px), no solo HTTP 200. 8/8 tests E2E siguen en verde tras los cambios de Home.
- Honesto: no se ha logrado (ni se puede, sin herramienta de generación de imágenes o fotografía real) una réplica pixel-perfect de una fotografía real de un brazo con pulseras — la mejora es de composición/estructura/profundidad dentro de lo generativo, documentado como tal en cada componente nuevo.

## AUDITORÍA VISUAL de /shop contra mockup real (p.14) — 2026-08-12
- [x] **Gap real confirmado**: el mockup tiene sidebar con filtros de Material y Precio (slider), además de categoría; la implementación previa solo tenía categoría+búsqueda+orden. Corregido: nuevo `ShopSidebarFilters.tsx` (checkboxes de 6 materiales reales + slider de precio máximo), estado real en la URL (`?material=...&maxPrice=...`), colapsable en `<details>` en mobile, sidebar fija desde `lg:`. `filterProducts()` extendido con `materials`/`maxPrice` reales sobre `product.materials`.
- [x] Verificado con Playwright real (no solo curl): 12→9 productos al activar "Baño de oro 18k", 9→2 al limitar a 10€, estado reflejado en la URL, captura visual confirma el layout de sidebar. Sin errores de página.
- [x] **Confirmado, no un gap**: tanto la p.13 (Best Sellers) como la p.19 (Producto) del PDF muestran reseñas/estrellas/UGC con nombres inventados ("Marta S.", "Ana G."...) — la PDP real correctamente NO los incluye. Documentado explícitamente en KNOWN_ISSUES.md como decisión intencional, no descuido.
- [x] 8/8 E2E en verde tras los cambios. Datos de prueba limpiados.

## Estado i18n (PAUSADO, no descartado)
La infraestructura real ES/EN/FR (`lib/i18n/`, diccionarios, selector funcional en Header/Footer/Search/Newsletter) quedó construida y compilando limpio antes de que el usuario pidiera parar para priorizar fidelidad visual. Cobertura actual: chrome global (header/footer/búsqueda) + parcial. Pendiente: el resto del copy específico de cada página. Se retoma cuando el usuario lo indique o al llegar de nuevo a esa fase.

## Colecciones: 3 → 6, cerrando un gap detectado y dejado abierto en sesión previa
- [x] **Gap real, ya conocido pero nunca resuelto**: `PROJECT_STATE.md` documentaba desde antes de esta sesión que la p.21 del PDF ("COLECCIONES") muestra 6 colecciones reales (RAÍCES, LUNAR, ORIGEN, ALMA, TIERRA, LUZ, con conteo de piezas por colección) frente a las 3 construidas (Raíces/Tricolor/Nocturna). Confirmado de nuevo con zoom 14x sobre el crop exacto. Cerrado ahora: `collections.ts` reescrito con las 6, los 12 productos reales redistribuidos 2 por colección (antes 2 productos — Hilos de Ometepe y Charm Cacao — no estaban en ninguna colección; ahora los 12 están cubiertos).
- [x] Copy de las 5 colecciones nuevas es texto propio honesto y descriptivo (el PDF solo da nombre+conteo, no taglines) — no se ha inventado ninguna afirmación de marca no verificable, mismo criterio que ya se aplicaba a "Tricolor"/"Nocturna".
- [x] **4 dependencias reales encontradas y corregidas** para que el rename no rompiera nada: `club/page.tsx` (niveles de fidelización usaban los nombres viejos), `colecciones/page.tsx` (metadata + titular hardcodeaba "tres formas"), `lib/looks.ts` (el campo `mood` de 2 looks apuntaba a colecciones que ya no existen, lo que habría roto el color de acento en Lookbook), `LookScene.tsx` (comentario desactualizado). Verificado que "Cadena Tricolor" (nombre de producto real) y el artículo de Journal sobre el tricolor venezolano NO se tocan — son sobre el motivo de bandera real, no sobre la colección renombrada.
- [x] Build genera las 6 rutas estáticas (`/colecciones/raices|lunar|origen|alma|tierra|luz`), las 6 devuelven 200 en servidor real (PID 15968), `/lookbook/noche-de-lago` confirmado mostrando el nuevo mood "Lunar" correctamente. 8/8 E2E en verde. Datos de prueba limpiados.
