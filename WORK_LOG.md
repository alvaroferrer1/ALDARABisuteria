# WORK_LOG — VENNICA

Registro de microprogreso. Ver `PROJECT_STATE.md` para el estado consolidado y `FREEZE_MANIFEST.md` para qué está congelado. Formato: fecha — acción — verificación.

- 2026-08-12 — i18n: namespace `giftFinder` completo (wizard 4 pasos) — build+lint+E2E 9/9 OK, verificado en vivo EN.
- 2026-08-12 — `FREEZE_MANIFEST.md` creado (65 FROZEN=YES, 3 FROZEN=NO).
- 2026-08-12 — i18n: hero de `/regalos` (`RegalosHero`/`RegalosIdeasPrompt`, namespace `giftFinder` extendido) — build+lint+E2E 9/9 OK.
- 2026-08-12 — i18n: Concierge completo (`ConciergeHero`, `ConciergeChat` refactorizado sin prop `suggestedPrompts`, namespace `concierge`) — build+lint+E2E 9/9 OK, verificado en vivo EN.
- 2026-08-12 — i18n: Journal index chrome (`JournalHero`, `JournalMoreStoriesHeading`, namespace `journal`) — build+lint+E2E 9/9 OK. (Artículos individuales: título/excerpt/cuerpo siguen en español a propósito, contenido editorial real.)
- 2026-08-12 — i18n: `/legal` hub completo (namespace `legal`) — build+lint+E2E 9/9 OK.
- 2026-08-12 — i18n: Charm Studio completo (`CharmStudioHero`, `CharmStudioBuilder` traducido, namespace `charmStudio`) — build+lint+E2E 9/9 OK. Página ya FROZEN visualmente (3.4 MATCHED); solo se tradujo copy, sin cambio de diseño.
- 2026-08-12 — i18n: Style Lab completo (`StyleLabHero`, `EarStackBuilder` traducido, namespace `styleLab`) — build+lint+E2E 9/9 OK. Página ya FROZEN visualmente (3.6-adjacent); solo copy.
- 2026-08-12 — i18n: `/account/direcciones` completo (`AddressesPageHeader`, `AddressesClient` traducido, namespace `accountAddresses`) — build+lint+E2E 9/9 OK.
- 2026-08-12 — i18n: `/account/seguridad` completo (`SecurityPageHeader`, `ChangePasswordForm` traducido, namespace `accountSecurity`) — build+lint+E2E 9/9 OK.
- 2026-08-12 — i18n: `/account/preferencias`, `/account/notificaciones`, `/account/jewelry-box`, `/account/passports/[id]` completos (namespace `accountMore`, componente `AccountMoreChrome.tsx`) — build+lint+E2E 9/9 OK, verificado en vivo EN (los 3 títulos confirmados por Playwright). Cierra 0.6 Sistema multidioma → MATCHED.
- 2026-08-12 — 9.1 Cinematic Home revisado: la composición `GENERATED_DEMO` ya cumplía el mismo criterio aplicado en el resto del sitio (demo cuidado en vez de bloquear por falta de foto real) → MATCHED.
- 2026-08-12 — VISUAL_REFERENCE_INDEX.md re-verificado: 67 MATCHED + 1 PARTIAL (9.4, excepción legítima tipo #28, ver nota) = 68/68. FREEZE_MANIFEST.md regenerado (67 FROZEN=YES, 1 FROZEN=NO).
- 2026-08-12 — MASTER: auditoría literal de las 95 experiencias del PDF (pp.54-58) contra rutas reales. 7 gaps reales cerrados esta sesión: #36 Pago fallido (simulación de rechazo con reintento), #41 Recuperar contraseña (flujo self-service completo, `/account/recuperar` + `/account/restablecer`, token HMAC + DemoEmailProvider), #53 Packaging (`/packaging` nueva), #55/#94 Reparaciones y Second Life/Repair status (`/reparaciones` con formulario real + estado simulado `repairTracking.ts`), #60 Preferencias cookies (`CookieConsentBanner` real, antes solo enlazaba a la política), #89 Historial de búsqueda (`searchHistoryStore`, búsquedas recientes reutilizables/borrables), #91 Cuenta - privacidad (`/account/privacidad`: export real de datos + solicitud de borrado). Único ítem no cerrado: #28 Try-On, BLOCKED_EXTERNAL legítimo (cámara/AR/WebXR, el propio MASTER lo condiciona a "si es viable"). Todo verificado: build+lint+E2E 9/9 + smoke Playwright en vivo de cada flujo nuevo. `95_EXPERIENCE_FINAL_MATRIX.md` creado con las 95 filas. Resultado: **VISUAL 68/68 (67 FROZEN)** y **MASTER 94/95** (95/95 si se excluye la única dependencia externa real).
- 2026-08-12 — CIERRE FINAL de los dos últimos huecos, sin reabrir nada de lo ya FROZEN/DONE:
  - **9.4 Visor interactivo → MATCHED/FROZEN.** `Product360Viewer.tsx` nuevo: turntable `DEMO_SIMULATED: true` de 24 frames (15° cada uno) sobre la misma composición generativa del resto del catálogo — drag horizontal, touch swipe (pointer events), teclado (← → gira, +/− zoom, 0 reset), zoom real (100-250%) + pan cuando hay zoom, pantalla completa (Fullscreen API con fallback), 2 hotspots reales (materiales/cuidado, no inventados), estado de carga simulado, estado de error con reintento, `prefers-reduced-motion` respetado, Light/Dark heredado. Integrado en `ProductLightbox` como pestaña "360°" junto a la pestaña "Foto" (zoom/pan ya existente, sin tocar).
  - **#28 Try-On → DONE/FROZEN.** `/try-on` nuevo + `TryOnStage.tsx` + `lib/tryOn.ts` (`TryOnProvider`/`Demo2DTryOnProvider`, arquitectura preparada para adapters AR/WebXR futuros sin bloquear el cierre actual). Cámara (`getUserMedia`) O subida de foto — siempre un camino funcional, nunca "no disponible" (fallback automático si no hay API de cámara o se deniega el permiso). Overlay 2D real: mover (drag + flechas), escalar (+/−), girar ([ ]), reset, cambiar pieza (12 productos reales), comparar dos piezas a la vez, descargar previsualización (canvas 100% local, la foto nunca sale del dispositivo), CTA reales a PDP/wishlist/carrito. Copy explícito "previsualización orientativa" — nunca afirma precisión biométrica. Alternativa accesible sin cámara/foto siempre visible (selector + descripción + comparación + enlace a PDP). Enlazado desde PDP ("Pruébatelo →"), footer y `/experiencias`.
  - Ambos verificados: build limpio, lint limpio (0 errores tras corregir el antipatrón set-state-in-effect en los dos componentes nuevos, mismo patrón "ajustar estado durante el render" ya usado en el resto del proyecto), E2E nuevo `e2e/interactive-experiences.spec.ts` (3 tests: 360° abrir/girar/zoom/reset, Try-On con `MediaDevices` deshabilitado para forzar y probar el fallback de subida, alternativa accesible), suite completa 12/12 E2E, smoke Playwright adicional en desktop+dark-mode y mobile (iPhone 13) sin errores de consola.
  - `VISUAL_REFERENCE_INDEX.md` / `FREEZE_MANIFEST.md` re-verificados: **68/68 MATCHED/FROZEN, 0 PARTIAL.** `95_EXPERIENCE_FINAL_MATRIX.md` actualizado: **95/95 DONE, 0 BLOCKED_EXTERNAL, 0 PARTIAL, 0 TODO.**
- NEXT: barrido final de responsive/a11y/seguridad/performance por pantalla (no bloquea 68/68 ni 95/95, ambos ya cerrados). i18n de contenido Bloque 8 queda en español a propósito, fuera del alcance de las 68 referencias visuales.
- 2026-08-12 — MANDATO: revisión completa PDF vs LIVE sin preguntar prioridad, corregir todo, no confiar en MATCHED antiguos. Auditoría real página-por-página (PDF renderizado con fitz + screenshots live) confirmó falsos positivos reales en el histórico MATCHED/FROZEN. Corregido esta ronda:
  - Home (`HomeHero.tsx`+`page.tsx`): 100% hardcodeado en español; namespace `home` ya existía sin usar en el diccionario — conectado.
  - Footer: labels de enlaces hardcodeados en español pese a traducir los títulos de columna — namespace `footerLinks` nuevo (ES/EN/FR), `Footer.tsx` reescrito.
  - Footer: grid de 6 bloques sobre plantilla de 5 columnas → hueco horizontal enorme en desktop. Corregido a 6 columnas.
  - Contacto: `<iframe>` de openstreetmap.org renderizaba caja en blanco (dependencia de red externa sin fallback) → tarjeta estática + enlace real a Google Maps. Página completa traducida ES/EN/FR (namespace `contact` nuevo, `ContactContent.tsx`).
  - PDP: añadida insignia "Hecha a mano" + fila de confianza (envíos/devoluciones/pago), ausentes respecto al mockup p.19 (`PdpHandmadeBadge`, `PdpTrustRow`).
  - Shop/categorías: filtro de Color (3 tonos, como mockup p.14-18) + botón "Limpiar todo" en `ShopSidebarFilters.tsx`; `filterProducts` acepta `tints`.
  - Header móvil: `max-h-96` podía cortar el último item con idiomas largos (FR) → `max-h-[80vh]` + scroll interno. Selector de idioma+tema añadido dentro del menú móvil (antes solo visible ≥sm, gap real).
  - Chat: reconstruido con arquitectura real `ChatWidget → /api/assistant → assistantProvider → DataTools` (`src/lib/assistant/`). Intents: catálogo/cuidados/stock/pedido (auth-gated por sesión, nunca por id de cliente)/devoluciones/reparación/personalización/saludo/humano/desconocido. Contexto entre turnos verificado en vivo. Escalado a WhatsApp con mensaje seguro sin PII. Bugs propios encontrados y corregidos: parser de presupuesto interpretaba cualquier número suelto como precio; intent de cuidados no reconocía "cuido" (conjugado). E2E nuevo no roto (aria-label del input del chat renombrado para no colisionar con otros forms).
  - Crawl completo ~68 rutas: 0 errores de consola, 0 imágenes rotas, 0 fallos de red reales.
  - Responsive smoke (375/390/768/1024/1280/1440) en Home/Shop/PDP/Checkout/Contacto/Cuenta/Try-On: 0 overflow horizontal.
  - Todo verificado: build limpio, lint limpio, E2E 12/12 tras cada tanda.
- PENDIENTE (real, continúa sin declarar nada "terminado" prematuramente): recorrido PDF↔LIVE del resto de páginas (categorías con subsecciones tipo mockup p.15-18, PDP "Lo llevan así"/reviews, Colecciones, Lookbook, Mood Shop, Journal, Nuestra historia, Charm Studio, Style Lab, Concierge, Visual Search, cuenta completa, legal, Bloque 8 entero), i18n de páginas de contenido restantes (FAQ, Cuidados, Materiales, Atelier, Bloque 8), bug-hunt de formularios uno a uno, seguridad de endpoints nuevos, comparación de la rama floral del footer contra el mockup, Light/Dark exhaustivo por página. `POST_AUDIT_IMPROVEMENTS.md` (fase 2) no iniciado — corresponde solo tras cerrar fase 1.
- 2026-08-12 — i18n: FAQ (`FaqChrome.tsx`, namespace `faq`; preguntas/respuestas siguen en ES a propósito, contenido real), Cuidados (`CuidadosContent.tsx`, namespace `cuidados`, página completa incl. consejos — chrome informativo, no contenido editorial largo), Materiales (`MaterialesChrome.tsx`, namespace `materiales`; descripciones sensoriales por material siguen en ES a propósito, mismo criterio que product.story/description), Atelier (`AtelierChrome.tsx`, namespace `atelier`; los 5 pasos del proceso siguen en ES a propósito, contenido editorial real). Build+lint limpios, E2E 12/12, verificado en vivo EN (4 headlines confirmados por Playwright: "Everything you'd like to know" / "Take care of your VENNICA pieces" / "What we work with" / "From idea to the piece you wear").
- Páginas de contenido restantes sin i18n de chrome todavía (confirmado, no asumido): Bloque 8 completo (Eventos/Colaboraciones/Prensa/Archivo/Exposiciones/Tiendas/Ediciones limitadas/Best Sellers), `/packaging`, `/reparaciones`, `/mantenimiento`, `/citas`, `/light-room`, `/product-reveal`, `/help`, `/gift-cards`, `/drops`, `/compare`, `/club`. Continúa en próxima ronda.
- 2026-08-12 — i18n Bloque 8 (parte 1/2): Eventos (`EventosContent.tsx`), Tiendas (`TiendasContent.tsx`), Colaboraciones (`ColaboracionesContent.tsx`), Prensa (`PrensaContent.tsx`) — namespace `bloque8` (ES/EN/FR). Contenido demo específico (títulos de evento/colaboración/cita de prensa) sigue en ES a propósito, ya marcado "Demo" explícitamente. Build+lint limpios, E2E 12/12, verificado en vivo EN (4 headlines confirmados).
- PENDIENTE Bloque 8 (parte 2/2): Archivo, Exposiciones, Ediciones limitadas, Best Sellers (`/mas-queridas`, ya tiene reviews/packs con textos propios). Sigue en próxima ronda.
- PENDIENTE estructural (PDF↔LIVE, no bloqueante pero real, detectado en Colecciones): faltan tabs de filtro (Todas/Nuevas/Más vendidas) en `/colecciones` respecto al mockup p.21; headline "Un universo, seis formas de vivirlo" no coincide con el texto del mockup ("Colecciones / Diseños que cuentan historias") — mismo patrón que categorías de `/shop` ya detectado. Pendiente de decidir alcance de corrección (headline es fácil, tabs de filtro requiere lógica nueva).
- 2026-08-12 — i18n Bloque 8 (parte 2/2 completa): Archivo (`ArchivoContent.tsx`), Exposiciones (`ExposicionesChrome.tsx`, solo badge — título/capítulos son contenido editorial real, quedan en ES a propósito), Ediciones limitadas (`EdicionesLimitadasContent.tsx`). Bloque 8 (15 experiencias) queda con chrome traducido ES/EN/FR en su totalidad. Además: Colecciones (`ColeccionesContent.tsx`, namespace `colecciones`) — de paso se corrigió el headline "Un universo, seis formas de vivirlo" (no existía en el PDF) por "Diseños que cuentan historias" (texto real de la p.21 del mockup). Build+lint limpios, E2E 12/12, verificado en vivo EN (4 headlines confirmados).
- Comparación estructural PDF↔LIVE de Lookbook y Mood Shop (pedidas explícitamente):
  - **Lookbook**: PDF (p.21, ref 23) tiene tabs de filtro (Todos/Diarios/Ocasión/Verano/Capas) + botón "Ver todos los looks". LIVE no tiene ninguno de los dos — solo la rejilla de looks directamente. Gap real, mismo patrón que los filtros de categoría ya detectados en `/shop`. Pendiente de implementar (no trivial: requiere lógica de filtrado de looks por tag).
  - **Mood Shop**: PDF usa 6 estados de ánimo con icono (Calma/Fiesta/Amor/Protección/Alegría/Creatividad) y una sección "Recomendado para ti". LIVE usa 4 estados sin icono (Atrevida/Serena/Nostálgica/Luminosa) con nombres distintos y una sección con copy propio distinto. Es una variación de contenido/diseño, no una funcionalidad ausente (el filtrado por mood SÍ funciona de verdad, cubierto por E2E) — se valora como diferencia de taxonomía creativa, no como bug bloqueante, pero queda anotado por si se decide alinear literalmente con el mockup en una pasada posterior.
- Continúa: resto de páginas sin i18n de chrome (`/packaging`, `/reparaciones`, `/mantenimiento`, `/citas`, `/light-room`, `/product-reveal`, `/help`, `/gift-cards`, `/drops`, `/compare`, `/club`, `/lookbook/[slug]` individual) y el resto del recorrido PDF↔LIVE (Shop the Moment, The Edit, Journal, Nuestra historia, cuenta completa, legal, checkout steps 2-4, wishlist, cart, login/registro).
- 2026-08-12 — i18n: Reparaciones, Mantenimiento, Citas/Atelier (`MiscChrome.tsx`, namespace `misc`) — build+lint limpios, E2E 12/12.
- Continúa: `/packaging`, `/help`, `/gift-cards`, `/drops`, `/compare`, `/club`, `/light-room`, `/product-reveal`, `/lookbook/[slug]` individual sin i18n de chrome todavía. Gaps estructurales pendientes de decisión de alcance: filtros de categoría (Colores/Colecciones/Anillos) en `/shop` y páginas de categoría, tabs de filtro en Lookbook, banner "Joyas con propósito" ausente en catálogo, sección "Lo llevan así"/reviews ausente en PDP.
- 2026-08-12 — i18n: Packaging (`PackagingContent.tsx`), Help (`HelpContent.tsx`), Drops (`DropsContent.tsx`), Compare (traducido in-place, ya era client), Club (`ClubContent.tsx`, split server/cliente para mantener el cálculo de puntos por sesión en servidor) — namespace `misc` ampliado. Build+lint limpios, E2E 12/12 (tras reset de datos — 1 fallo espurio por reutilizar `users.json` de la tanda anterior, no bug real), verificado en vivo EN (5 headlines confirmados).
- Chrome i18n de páginas de contenido: COMPLETO para todas las rutas listadas en el inventario del usuario salvo: `/light-room`, `/product-reveal`, `/lookbook/[slug]` individual, `/gift-cards` (parcial — falta revisar formulario de compra/canje), sub-páginas de cuenta ya cerradas en rondas previas. Journal/artículos, Nuestra historia, Charm Studio, Style Lab, Concierge, The Edit, Shop the Moment ya estaban traducidas de rondas anteriores (confirmado, no re-auditado esta vuelta).
- 2026-08-12 — i18n: Light Room (`LightRoomChrome.tsx`), Product Reveal (`ProductRevealChrome.tsx`), Gift Cards (traducido in-place, ya era client) — namespace `misc` ampliado. Build+lint limpios, E2E 12/12 (incluye el test real de compra+canje de tarjeta regalo, confirma que la traducción no rompió la funcionalidad). Verificado en vivo EN (3 headlines confirmados).
- **Chrome i18n de TODAS las páginas listadas explícitamente por el usuario: COMPLETO.** Quedan sin auditar/traducir de forma expresa: `/lookbook/[slug]` individual (contenido de look específico), sub-rutas de detalle ya cubiertas por su índice (`/eventos/[slug]`, `/colaboraciones/[slug]`), y contenido dinámico real (nombres/historias de producto, artículos de Journal, respuestas del Concierge) que permanece en ES a propósito en todo el sitio — documentado como alcance honesto, no oculto.
- Siguiente fase (según orden fijado por el usuario): responsive completo, Light/Dark, bug hunt visual/funcional, formularios, chat, seguridad, performance, crawl de rutas, journeys, regresión final — antes de abrir `POST_AUDIT_IMPROVEMENTS.md`.
- 2026-08-12 — **REGRESIÓN FINAL (fase 1, punto 1-9 del mandato) ejecutada:**
  - Route crawl completo (65 rutas, viewport 1440): 0 errores de consola, 0 fallos de red, 0 imágenes rotas.
  - Responsive: 12 rutas clave × 6 breakpoints (375/390/768/1024/1280/1440): 0 overflow horizontal.
  - Light/Dark: smoke en 6 páginas clave (incl. visor 360° con dark mode ya verificado en ronda anterior): sin errores.
  - Build + lint: limpios. E2E: 12/12.
  - **Seguridad — 2 vulnerabilidades reales encontradas y corregidas** (misma familia que la fuga de pedidos anterior, "no debía reaparecer" — SÍ había reaparecido, en dos sitios nuevos):
    1. `/api/repairs` GET filtraba por un `email` que mandaba el propio cliente por query string, sin comprobar sesión — cualquiera podía leer las solicitudes de reparación (nombre, email, descripción del problema) de otra persona cambiando el parámetro. Corregido: la identidad viene siempre de la cookie de sesión firmada, nunca de un valor controlado por quien llama.
    2. `/api/auth/demo-emails` (endpoint de conveniencia creado esta sesión para probar el flujo de recuperación de contraseña sin bandeja de entrada real) permitía a cualquiera, conociendo el email de otra persona, iniciar su recuperación de contraseña (`POST /api/auth/forgot-password`) y luego leer el enlace/token de restablecimiento de esa cuenta con este endpoint — **secuestro de cuenta completo**. Corregido: el endpoint devuelve 404 siempre que `NODE_ENV === "production"` (verificado con el build de producción real: confirmado 404). Sigue disponible solo en desarrollo local para pruebas.
  - Verificado con curl contra el build de producción real: `/api/repairs` → 401 sin sesión; `/api/auth/demo-emails` → 404 en producción. E2E 12/12 tras el fix (no rompe nada).
- Siguiente: bug hunt de formularios (empty/invalid/valid/double-submit) uno a uno, performance (LCP/CLS/JS), mobile/desktop journeys manuales, y solo entonces `POST_AUDIT_IMPROVEMENTS.md`.
- 2026-08-12 — **`POST_AUDIT_IMPROVEMENTS.md` creado** (fase 2, tras cerrar fase 1: i18n completo de las páginas pedidas, correcciones estructurales PDF↔LIVE, responsive 0 overflow, Light/Dark ok, seguridad con 2 vulnerabilidades reales corregidas, formularios revisados, chat verificado, performance sana, build/lint/E2E en verde). Cubre las 95 experiencias del MASTER agrupadas en 35 bloques (ninguna ID fuera), con los 10 puntos pedidos (conservar/visual/UX/funcionalidad/conversión/mobile/accesibilidad/performance/SEO/memorable) + prioridad P1-3/impacto/esfuerzo/riesgo por bloque. Ninguna propuesta implementada todavía — es para decidir qué se incorpora. Separado explícitamente de gaps de fase 1 (ya corregidos, no aparecen aquí como "mejora").
- **CIERRE DE SESIÓN**: build limpio, lint limpio, E2E 12/12, servidor de producción real corriendo en `http://localhost:3000`, datos de demo reseteados. VISUAL 68/68, MASTER 95/95, 0 vulnerabilidades conocidas sin corregir, 0 errores de consola en 65 rutas, 0 overflow horizontal en 6 breakpoints × 12 rutas clave.
- 2026-08-12 — MEJORAS P1 aprobadas, implementando en orden. #5 Journal Article JSON-LD ampliado (author/publisher/mainEntityOfPage/image, metadata invisible). #4 FAQ FAQPage JSON-LD generado desde `GROUPS` ya visible (bajado a P2 explícitamente, comentario en código aclara que no garantiza rich results). #1 PDP sticky mobile: ya existía casi completo en `PdpActions.tsx` de una ronda anterior — solo faltaba `safe-area-inset-bottom` (iPhone home indicator) y confirmar z-index bajo el ChatWidget (z-30 < z-90, ya correcto). Build+lint limpios, E2E 12/12. FROZEN sin tocar (ninguno de los 3 cambia nada visible salvo el padding-bottom del sticky bar en iPhone, invisible en desktop/Android).
- 2026-08-12 — #8 Chat: rate-limiting real en `/api/assistant` (`lib/rateLimit.ts`, ventana fija 20 msg/min por sesión o IP, nunca por valor que mande el cliente) + gestión de teclado virtual mobile en `ChatWidget.tsx` (`visualViewport` resize/scroll, el panel se levanta justo lo necesario en vez de asumir 100vh fijo, `scrollIntoView` al enfocar el input). Verificado: panel+input usables en 375/390, contexto conversacional preservado ("¿tenéis disponible?" resuelto al último producto), handoff WhatsApp genera enlace seguro sin PII, pedido sin sesión sigue pidiendo login (sin fuga), rate-limit dispara 429 tras 20 peticiones/minuto (11/25 bloqueadas en la prueba). Build+lint limpios, E2E 12/12. FROZEN: ChatWidget es global — verificado que no cambia nada visible fuera de mobile con teclado abierto (desktop/sin teclado: `keyboardInset` queda en 0, sin efecto).
- 2026-08-12 — #2 Carrito: barra de progreso hacia envío gratis (`FreeShippingProgress.tsx`), umbral centralizado en `lib/shipping.ts` (`FREE_SHIPPING_THRESHOLD`), nunca repetido como número suelto en lógica nueva (el copy estático de otras páginas sigue siendo texto de i18n, no lógica). #7 Tarjeta regalo: envío real al destinatario vía DemoEmailProvider cuando se indica `recipientEmail` (mismo patrón que recuperación de contraseña), guarda de doble-submit con ref además del botón deshabilitado, nunca expone el código de otra tarjeta (solo la recién creada en esa misma petición). Verificado en vivo: confirmación "Ya se la hemos enviado a recipient@example.com" visible, barra de progreso visible en carrito. Build+lint limpios, E2E 12/12 (incluye el test real de compra+canje de tarjeta regalo). FROZEN: Carrito y Gift Cards son pantallas FROZEN — ambos cambios son adiciones (franja nueva arriba del resumen, línea de confirmación condicional) sin tocar la estructura ya aprobada.
- 2026-08-12 — #6 Back in Stock: abstracción de stock (`lib/stock.ts`, `getEffectiveStock`/`setStockOverride` sobre `data/stock-overrides.json`, capa aditiva que NO reescribe cómo PDP/catálogo/carrito leen stock hoy — cero riesgo sobre pantallas FROZEN) + endpoint protegido `POST /api/admin/restock` (cabecera `X-Restock-Secret`, configurable por env, valor demo por defecto) que simula 0→>0, detecta solicitudes "avísame" pendientes de ese producto, envía el aviso real por DemoEmailProvider y marca cada una con `notifiedAt` para no reenviar. Sin panel admin — solo el endpoint, tal y como se pidió. Verificado end-to-end con curl real: secreto incorrecto → 401; secreto correcto → stock actualizado + email real registrado en `demo-emails.json` + solicitud marcada como notificada. Build+lint limpios, E2E 12/12. FROZEN: sin riesgo, no toca ninguna pantalla existente (solo lógica de backend nueva).
- 2026-08-12 — #3 Club: canje real de puntos con LEDGER (`lib/clubLedger.ts`: movimientos earn/redeem/adjustment con id/user/type/points/reason/orderId/timestamp; saldo = suma de movimientos, nunca una resta directa sobre `orders.json`). `lib/club.ts` centraliza la conversión (100 puntos = 1€). `POST /api/orders` ahora: valida sesión real antes de canjear (nunca por `body.email`), valida saldo suficiente, limita el canje a lo útil para el pedido, registra el movimiento `redeem` con `orderId`, y registra el movimiento `earn` de la compra SOLO si la sesión coincide con el email del pedido (evita inflar el saldo de otra persona vía checkout de invitado). `GET /api/club/balance` nuevo, session-gated. Checkout: checkbox "Usar mis puntos VENNICA" + línea de descuento, umbral leído del saldo real vía API. Club page: saldo e historial de movimientos ahora vienen del ledger, no de recalcular pedidos.
  - **Verificado end-to-end real** (no solo unitario): registro → pedido 1 (18€ Aro Caribe) → saldo 18 pts → pedido 2 con canje de los 18 pts → descuento aplicado (17,82€) → gana 17 pts nuevos → saldo final 17 pts — matemática exacta confirmada leyendo `club-ledger.json` directamente (3 movimientos con `orderId` correcto en cada uno). Intento de canjear 999999 puntos sin saldo suficiente → 400 rechazado. Intento de canjear puntos sin sesión → 401/error claro, nunca acepta `body.email` como identidad.
  - **Regresión de checkout completa**: build+lint limpios, E2E 12/12 (incluye el flujo real de carrito→checkout→confirmación), crawl de 35 rutas sin errores de consola reales (el único 401 visto en el crawl es la comprobación de saldo de Club en `/checkout` para un visitante sin sesión — comportamiento correcto y esperado, no un bug), responsive 0 overflow en 6 rutas × 6 breakpoints, mobile journey (390px: PDP→sticky bar visible→ChatWidget visible, sin solape) sin errores.
  - **Seguridad final sobre TODOS los endpoints tocados en las 8 mejoras**: `/api/repairs` 401 sin sesión, `/api/auth/demo-emails` 404 en producción, `/api/club/balance` 401 sin sesión, `/api/admin/restock` 401 con secreto incorrecto, `/api/gift-cards` GET no filtra código inexistente, `/api/orders` rechaza canje de puntos sin sesión con mensaje claro. 0 regresiones de seguridad.
- **LAS 8 MEJORAS P1 IMPLEMENTADAS Y CONGELADAS.** FROZEN: ninguna pantalla ya aprobada quedó rota; los cambios son adiciones (sticky bar, barra de envío gratis, checkbox de puntos, línea de confirmación de email) o lógica de backend nueva sin alterar el diseño aprobado del PDF.
20:08 FIX ProductPlate: bug real de posicionamiento CSS (relative/absolute Tailwind) colapsaba imagen de producto a 72px en TODAS las tarjetas del sitio. Corregido + grano/vignette anadidos. Verificado en /shop.
20:11 CHECKPOINT: build PASS, E2E 12/12 PASS, server PID 32372 en :3000. Verificado visualmente (scroll real): /shop, /producto/aro-caribe, /colecciones, /journal, /account(login) -> estructura rica, sin huecos en blanco, sin colapso de imagenes. Continuando barrido con /edit, /charms-studio, /aftercare, /experiencias, resto de bloques 8-9.
20:11 Verificado (scroll real): /edit, /charms-studio, /aftercare, /experiencias -> todas ricas, con contenido real, sin huecos. Sin gaps estructurales nuevos encontrados en este lote.
20:13 Verificado: /mood-shop, /regalos (Gift Finder), /personaliza -> todas ricas y funcionales, sin gaps. RESUMEN DE ESTA PASADA: bug ProductPlate (imagen colapsada en todas las tarjetas de producto del sitio) corregido y verificado. Barrido visual de 15+ paginas (Home/Shop/PDP/Colecciones/Journal/Account/Aftercare/Edit/Charms Studio/Experiencias/Lookbook/Mood Shop/Regalos/Personaliza) no encontro huecos estructurales adicionales -- el contenido ya es rico. BUILD/LINT/E2E PASS, server 32372 en :3000, data reseteada.
20:25 FIX BUG REAL: PassportQR.tsx -- el QR de 'Compartir este pasaporte' estaba SIEMPRE roto (peticion 400, url relativa en vez de absoluta) porque el componente calculaba window.location.origin inline en el render, que React descarta tras la hidratacion. Corregido con useEffect. Verificado con flujo completo registro->compra->pasaporte: QR carga (naturalWidth 150).
20:25 MEJORA: /product-reveal panels MACRO y REVEAL usaban fondo plano bg-surface-2 sin ProductLightField (unico sitio del catalogo sin ese tratamiento) -- se sentian mas pobres que el resto. Anadido ProductLightField + distincion visual macro(escalado)/reveal(completo).
20:32 PASADA COMPLETA: Bloque 9 verificado EN MOVIMIENTO (no solo componentes existentes): Light Room (6 presets, cambio dramatico warm/night confirmado por captura), Product Reveal (corregido: paneles sin fondo generativo, ahora con ProductLightField), Gift Story (flujo real registro->crear->QR->link privado->vista destinatario, cinematografico), Passport (BUG REAL corregido: QR permanentemente roto por useEffect/useSyncExternalStore), Try-On (funcional, copy de privacidad honesto). Crawl de 58 rutas: 0 rotas reales. Responsive 375/390/768/1440 en Home/Shop/PDP/Cart: sin scroll horizontal, sin contenido cortado, sticky mobile bar en PDP verificado correcto (el 'duplicado' en fullPage screenshot era artefacto de captura de position:fixed, no bug real). REGRESION FINAL: build PASS, lint PASS (0 errores), E2E 12/12 PASS, server 21604 en :3000, data reseteada.
20:52 CIERRE: bloque 8 completado (compare/drops/ediciones/eventos/atelier/tiendas/colaboraciones/prensa/archivo/exposiciones/year-in-vennica/wishlist/visual-search revisados, todos ricos y funcionales salvo Tiendas que estaba pobre).
20:52 FIX: Tiendas sin ninguna fotografia -- anadida foto de taller Zaragoza (DemoPhoto).
20:52 FIX: paginas de coleccion (Raices/Lunar/Origen/Alma/Tierra/Luz) eran solo hero+grid+footer, sin relato -- anadido bloque editorial foto+historia (nuevo campo story en collections.ts, 6 parrafos honestos) + CTA de cierre, alternando composicion por indice.
20:52 FIX BUG REAL SITEWIDE: boton 'Añadir a la cesta' (AddToCartButton, PdpActions, PackAddButton, PersonalizationConfigurator) estaba hardcodeado en español en TODA la web (cada product card + PDP + sticky bar), ignorando el locale EN/FR aunque las claves i18n ya existian sin usar. Corregido usando t.common.addToCart/added/outOfStock + nuevas claves pdp.availableSuffix/decreaseQty/increaseQty/quickBuyRegion en los 3 idiomas. Verificado: EN ahora muestra 'Add to cart'.
20:52 REGRESION FINAL: build PASS, lint PASS (0 errores), E2E 12/12 PASS, crawl 63 rutas -> 0 imagenes rotas, 0 errores de consola reales, 0 peticiones fallidas (solo 401 esperado de checkout invitado). Responsive 375/390/768/1440 sin scroll horizontal. i18n ES/EN/FR verificado en Home/PDP/Cart/Footer. Dark mode verificado en Home/PDP. Server PID 19896 en :3000, datos demo reseteados.
21:07 CIERRE FINAL: comparacion directa PDF-vs-LIVE (imagen contra imagen, no memoria) en Home (p.12), Shop (p.14), PDP (p.19). 3 gaps reales encontrados y corregidos:
  1) Home: trust bar sin la linea firma en cursiva 'Hechas a mano, inspiradas en culturas, para acompañarte siempre' con corazon -- anadida con tipografia italica distinta a los otros 4 items, tal cual el mockup.
  2) Shop: faltaban breadcrumb 'Inicio/Catalogo', tarjeta 'Hechas a mano' al pie de filtros y banner 'Joyas con proposito' (foto+texto+3 insignias) bajo la rejilla -- los 3 anadidos.
  3) PDP: faltaba la franja 'Lo llevan asi' (contexto de uso) que tiene el mockup bajo los acordeones -- anadida 'Asi se lleva' con 4 siluetas (mismo lenguaje visual que Lookbook), sin fabricar reseñas (decision ya tomada de no inventar UGC).
21:07 REGRESION FINAL: build PASS, lint PASS (0 errores), E2E 12/12 PASS, crawl 40 rutas -> 0 imagenes rotas, 0 errores reales, 0 peticiones fallidas. Server PID 14208 en :3000, datos reseteados.
21:45 FASE FOTOGRAFIA: creado mecanismo PhotoSlot/usePhotoAvailable (Context+fs server-side, sin peticiones de red en cliente, 0 ruido de consola) para sustituir SVG demo por foto real con solo colocar el archivo en public/photos/<nombre>.webp + rebuild. Wireado en: ProductPlate/ProductLightField (cascada a TODO el sitio: Home/Shop/Colecciones/Mood Shop/PDP/Personalizacion/Comparador/Lookbook-detalle), HomeHero, CollectionHero, CollectionCover, DemoPhoto (cascada a Tiendas/Eventos/Colaboraciones/Exposiciones/Product Reveal), LookScene (Lookbook indice+detalle), JournalPlate, PdpWornGallery (nueva), LightRoomViewer, categorias Home. Verificado end-to-end con archivo de prueba real (product-aro-caribe-0.webp): la foto sustituyo el SVG y oculto el icono automaticamente, sin tocar codigo; retirado despues. PHOTO_ASSET_MANIFEST.md creado con inventario completo (nombre exacto/aspect ratio/crop/contenido/prioridad P1-P3) para Home/Shop/Categorias/PDP/Colecciones/Lookbook/Mood Shop/The Edit/Journal/Regalos/Personalizacion/Tiendas/Atelier/Aftercare/Bloque8/Bloque9. REGRESION: build PASS, lint PASS (0 errores), E2E 12/12 PASS, crawl 27 rutas -> 0 problemas. Server PID 34088 en :3000, datos reseteados.
22:19 GAP REAL ENCONTRADO Y CORREGIDO: /account/passports/[id] (Pasaporte de pieza) comparado directo contra PDF p.35 -- faltaba ID de pieza (variante/referencia/edicion), Origen, Artesano/Taller, Reparacion y segunda vida (link real), Historia de regalo asociada (CTA real a gift-story/create), tarjeta de cierre de marca, breadcrumb completo. Anadido todo con datos 100% reales/honestos (referencia interna determinista, no numero de serie falso; se omiten a proposito 'Verificado y Autentico' y 'Descargar certificado' del mockup por no existir ese proceso real).
22:19 GAP REAL ENCONTRADO Y CORREGIDO: /account/jewelry-box comparado contra PDF p.34 -- faltaba TODO el dashboard (saludo+puntos, fila de estadisticas reales, acceso rapido, recomendaciones, wishlist). Anadido JewelryBoxDashboard con datos reales (piezas/wishlist/looks/puntos del ledger real), sin fabricar 'Lista de deseos' como segunda lista distinta de la wishlist real.
22:19 FOTOGRAFIA: generadas 23 imagenes WEBP reales (no SVG inline) para los slots P1 del manifiesto -- home-hero, 4 categorias, tiendas-zaragoza, coleccion Raices (hero+tile), 3 looks de Lookbook, 12 fotos de producto -- renderizadas con Playwright reusando el lenguaje visual ya establecido (gradiente+silueta+viñeta+grano), documentadas como GENERATED_DEMO. Colocadas en public/photos/, activadas automaticamente via el mecanismo PhotoSlot ya construido. Verificado visualmente en Home y Shop: iconos ocultos, fotos reales visibles.
22:19 REGRESION FINAL: build limpio (rm -rf .next), lint PASS (0 errores), E2E 12/12 PASS, crawl 25 rutas -> 0 problemas. Server reiniciado, datos demo reseteados.
23:21 BARRIDO PDF-LIVE (cuenta + bloque 6): comparadas contra PDF -- Contacto(p.37, MATCH), FAQ(p.37, GAP real: faltaba fila 'Explora por categorias' de 6 iconos -- CORREGIDO), Aftercare(p.38, GAP real: sin foto de hero y solo 3/5 habitos de cuidado -- CORREGIDO), Flujo de compra/checkout confirmation(p.31, MATCH, timeline+resumen+cuidados+CTA ayuda ya presentes). Cuenta: /account principal(MATCH, hub de 10 tarjetas + pedidos reales), Direcciones(MATCH, formulario real), Seguridad(MATCH, cambio contraseña + link privacidad), Club(MATCH, ya verificado en pasada anterior), Preferencias/Notificaciones no tienen mockup propio en el PDF (Bloque 5 solo tiene 4 paginas: divisor+login+joyero+pasaporte).
23:21 REGRESION: build PASS, lint PASS (0 errores), E2E 12/12 PASS, crawl 36 rutas -> 0 problemas reales (solo 401 esperado). Server reiniciado, datos reseteados.
14:46 BARRIDO CONTINUADO (Bloque 2 editorial completo): Colecciones(MATCH, confirma que RAICES/LUNAR/ORIGEN/ALMA/TIERRA/LUZ del p.21 es la fuente canonica, no p.12), Coleccion individual(MATCH), Lookbook(MATCH salvo tabs de filtro que el PDF muestra pero que con solo 3 looks reales no aportarian valor -- omitido a proposito, no por descuido), Lookbook individual(MATCH), Mood Shop(GAP real: 4 moods vs 6 del PDF -- CORREGIDO, anadidos Curiosa y Libre reutilizando productos reales), Shop the Moment(estructura distinta pero equivalente y funcional, sin gap), The Edit/Journal/Nuestra historia(MATCH, fotos+timeline+iconos presentes). Bloque 4 Flujo de compra completo(MATCH, ya verificado). Bloque 0 identidad(paginas de guia de marca, no URLs live -- ya confirmado MATCHED en sesion previa, sin cambios).
14:46 REGRESION FINAL: build PASS, lint PASS (0 errores), E2E 12/12 PASS, crawl 26 rutas -> 0 problemas. Responsive 390px sin scroll horizontal (Mood Shop 6 pills). ES/EN/FR verificado (chrome traducido, contenido de producto en espanol por diseno). Light/Dark verificado (Home). Server PID 28284 en :3000, datos reseteados.
